---
name: nazomatic-mobile-first-ux-overhaul
description: UXベストプラクティスに基づいて画面を根本改善し、提案だけでなく実際にコードを修正するスキル。ユーザーが特定画面のレビュー・再設計・導線改善・モバイル最適化を求めるときに使う。大胆なレイアウト変更が必要なケースにも対応し、Nazomaticの既存デザインルール（ダークグラデーション + purpleアクセント）に準拠する。
---

# Nazomatic Mobile-First UX Overhaul

## 固定コンテキスト
- ターゲットユーザー: 20代〜30代の謎解き好き、ゲーム操作に慣れている層
- デバイス方針: スマホ優先（PCも崩さない）
- デザインルール:
  - メイン: `bg-gradient-to-b from-gray-900 to-gray-800 text-white`
  - アクセント: `purple-400`
- 既存システムのUIトーンとコンポーネントを優先して再利用する

## 受け付ける最小入力
- 必須（どちらか1つ）:
  - 対象ルート（例: `/blank25`）
  - 対象ファイル（例: `src/components/blank25/problem-list.tsx`）
- 任意:
  - 追加要望（例: CTAを強調したい）
  - スクリーンショット

入力が不足する場合は、会話コンテキストの開いているファイルや対象機能から合理的に補完して進める。

## 実行手順
1. 対象画面と関連ファイルを特定する（ルート指定時は `src/app` と関連 `src/components` を探索）。
2. 現状UIを診断する。
3. 次の観点で問題を抽出する。
   - 情報設計
   - 認知負荷
   - 操作導線
   - UIの一貫性
   - モバイル適応
   - 視覚階層
   - アクセシビリティ
   - エラー/空状態
4. 改善案を3〜5件に絞る（影響が大きい順）。
5. 小手先の調整よりUX効果を優先し、必要なら大胆にレイアウト再設計する。
6. 提案で止めず、必ずコードを修正する。
7. スマホ表示を最優先で整え、PC表示の破綻を防ぐ。
8. 推測と事実を分けて説明する。

## 出力形式
A. 重大課題トップ3（影響度/頻度/修正コスト）  
B. 実装方針（根本改善の方針）  
C. 実装結果（変更ファイルごとの要点）  
D. マイクロコピー改善（Before/After）  
E. QAチェックリスト（スマホ/PC、アクセシビリティ、空状態、エラー状態）

## 重要ルール
- レビューのみで終了しない。必ず実装まで行う。
- カラー/トーンは固定コンテキストを厳守する。
- 既存のデザインシステムから大きく乖離する新規スタイルの乱立を避ける。
